<Window x:Class="CorporateNetworkWPF.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        Height="150" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Prompt}" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" Text="{Binding Answer, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="5" 
                    IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="75" Margin="5" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>